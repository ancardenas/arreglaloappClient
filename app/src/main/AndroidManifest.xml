<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Arreglalo.arreglalo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Arreglaloapp">
        <activity android:name=".clietnService"></activity>
        <activity android:name=".initSesion" />
        <activity android:name=".Facil" />
        <activity
            android:name=".cotizacion"
            android:theme="@style/Theme.ArreglaloappNobar" />
        <activity
            android:name=".finalService"
            android:theme="@style/Theme.ArreglaloappNobar" />
        <activity
            android:name=".confirmService"
            android:theme="@style/Theme.ArreglaloappNobar" />
        <activity
            android:name=".pincipalServices"
            android:theme="@style/Theme.ArreglaloappNobar" />
        <activity android:name=".mapaintento1" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".direction" />
        <activity android:name=".perm_dir" />
        <activity android:name=".per_data" />
        <activity android:name=".cod_seg" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".load"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>